# android/gradle.properties
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.enableJetifier=true

# Release signing values are read from android/keystore.properties (NOT
# committed — see BUILD.md). Do not hardcode keystore passwords here.
