// android/variables.gradle
// Shared version numbers referenced by app/build.gradle and any Capacitor
// plugin modules pulled in via capacitor.settings.gradle (regenerated by
// `npx cap sync android`).

ext {
    minSdkVersion = 26
    compileSdkVersion = 34
    targetSdkVersion = 34
    androidxActivityVersion = '1.9.0'
    androidxAppCompatVersion = '1.7.0'
    androidxCoreVersion = '1.13.1'
    androidxFragmentVersion = '1.8.1'
    coordinatorLayoutVersion = '1.2.0'
    androidxWebkitVersion = '1.11.0'
}
