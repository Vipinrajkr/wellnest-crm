# Dependencies
node_modules/

# Capacitor-generated (regenerated by `npx cap sync android`)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/capacitor.build.gradle
android/capacitor-cordova-android-plugins/
android/capacitor.settings.gradle
android/local.properties

# Gradle / Android Studio build output
android/.gradle/
android/app/build/
android/build/
android/.idea/
*.iml

# Release signing — never commit the keystore or its passwords.
# See BUILD.md for how to generate keystore.properties + the .jks/.keystore
# file locally; both stay on the signer's machine only.
android/keystore.properties
*.jks
*.keystore

# OS cruft
.DS_Store
Thumbs.db
